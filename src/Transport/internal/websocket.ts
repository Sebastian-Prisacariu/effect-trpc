/**
 * WebSocket transport implementation
 * @internal
 */

import { Layer } from "effect"
import { Transport, type WebSocketOptions } from "../index.js"

// =============================================================================
// Implementation (stub - to be implemented)
// =============================================================================

export const webSocketTransport = (
  _url: string,
  _options?: WebSocketOptions
): Layer.Layer<Transport, never, never> => {
  // TODO: Implement WebSocket transport
  throw new Error("WebSocket transport not implemented yet")
}
