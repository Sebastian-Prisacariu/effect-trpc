import * as Context from "effect/Context";
import * as Effect from "effect/Effect";
import type { ParseError } from "effect/ParseResult";
import { type Pipeable } from "effect/Pipeable";
import * as Schema from "effect/Schema";
import * as Stream from "effect/Stream";
import * as Rpc from "./Rpc.js";
/**
 * @since 1.0.0
 * @category type ids
 */
export declare const TypeId: unique symbol;
/**
 * @since 1.0.0
 * @category type ids
 */
export type TypeId = typeof TypeId;
/**
 * @since 1.0.0
 * @category refinements
 */
export declare const isRpcRouter: (u: unknown) => u is RpcRouter<any, any>;
/**
 * @since 1.0.0
 * @category models
 */
export interface RpcRouter<Reqs extends Schema.TaggedRequest.All, R> extends Pipeable {
    readonly [TypeId]: TypeId;
    readonly rpcs: ReadonlySet<Rpc.Rpc<Reqs, R>>;
}
/**
 * @since 1.0.0
 * @category models
 */
export declare namespace RpcRouter {
    /**
     * @since 1.0.0
     * @category models
     */
    type Context<A extends RpcRouter<any, any>> = A extends RpcRouter<infer Req, infer R> ? R | Schema.SerializableWithResult.Context<Req> : never;
    /**
     * @since 1.0.0
     * @category models
     */
    type ContextRaw<A extends RpcRouter<any, any>> = A extends RpcRouter<infer Req, infer R> ? R | Schema.Serializable.Context<Req> : never;
    /**
     * @since 1.0.0
     * @category models
     */
    type Request<A extends RpcRouter<any, any>> = A extends RpcRouter<infer Req, infer _R> ? Req : never;
    /**
     * @since 1.0.0
     * @category models
     */
    type Response = [
        index: number,
        response: Schema.ExitEncoded<any, any, unknown> | ReadonlyArray<Schema.ExitEncoded<any, any, unknown>>
    ];
    /**
     * @since 1.0.0
     * @category models
     */
    type ResponseEffect = Schema.ExitEncoded<any, any, unknown> | ReadonlyArray<Schema.ExitEncoded<any, any, unknown>>;
}
/**
 * @since 1.0.0
 * @category constructors
 */
export declare const make: <Rpcs extends ReadonlyArray<Rpc.Rpc<any, any> | RpcRouter<any, any>>>(...rpcs: Rpcs) => RpcRouter<Rpc.Rpc.Request<Extract<Rpcs[number], {
    readonly [Rpc.TypeId]: Rpc.TypeId;
}>> | RpcRouter.Request<Extract<Rpcs[number], {
    readonly [TypeId]: TypeId;
}>>, Rpc.Rpc.Context<Extract<Rpcs[number], {
    readonly [Rpc.TypeId]: Rpc.TypeId;
}>> | RpcRouter.Context<Extract<Rpcs[number], {
    readonly [TypeId]: TypeId;
}>>>;
/**
 * @since 1.0.0
 * @category context
 */
export declare const provideServiceEffect: {
    /**
     * @since 1.0.0
     * @category context
     */
    <I, S, E, R2>(tag: Context.Tag<I, S>, effect: Effect.Effect<S, E, R2>): <Reqs extends Schema.TaggedRequest.All, R>(self: RpcRouter<Reqs, R>) => RpcRouter<Reqs, Exclude<R, I> | R2>;
    /**
     * @since 1.0.0
     * @category context
     */
    <Reqs extends Schema.TaggedRequest.All, R, I, S, E, R2>(self: RpcRouter<Reqs, R>, tag: Context.Tag<I, S>, effect: Effect.Effect<S, E, R2>): RpcRouter<Reqs, Exclude<R, I> | R2>;
};
/**
 * @since 1.0.0
 * @category context
 */
export declare const provideService: {
    /**
     * @since 1.0.0
     * @category context
     */
    <I, S>(tag: Context.Tag<I, S>, service: S): <Reqs extends Schema.TaggedRequest.All, R>(self: RpcRouter<Reqs, R>) => RpcRouter<Reqs, Exclude<R, I>>;
    /**
     * @since 1.0.0
     * @category context
     */
    <Reqs extends Schema.TaggedRequest.All, R, I, S>(self: RpcRouter<Reqs, R>, tag: Context.Tag<I, S>, service: S): RpcRouter<Reqs, Exclude<R, I>>;
};
/**
 * @since 1.0.0
 * @category combinators
 */
export declare const toHandler: {
    /**
     * @since 1.0.0
     * @category combinators
     */
    (options?: {
        readonly spanPrefix?: string;
    }): <R extends RpcRouter<any, any>>(self: R) => (u: unknown) => Stream.Stream<RpcRouter.Response, ParseError, RpcRouter.Context<R>>;
    /**
     * @since 1.0.0
     * @category combinators
     */
    <R extends RpcRouter<any, any>>(self: R, options?: {
        readonly spanPrefix?: string;
    }): (u: unknown) => Stream.Stream<RpcRouter.Response, ParseError, RpcRouter.Context<R>>;
};
/**
 * @since 1.0.0
 * @category combinators
 */
export declare const toHandlerNoStream: {
    /**
     * @since 1.0.0
     * @category combinators
     */
    (options?: {
        readonly spanPrefix?: string;
    }): <R extends RpcRouter<any, any>>(self: R) => (u: unknown) => Effect.Effect<RpcRouter.ResponseEffect, ParseError, RpcRouter.Context<R>>;
    /**
     * @since 1.0.0
     * @category combinators
     */
    <R extends RpcRouter<any, any>>(self: R, options?: {
        readonly spanPrefix?: string;
    }): (u: unknown) => Effect.Effect<RpcRouter.ResponseEffect, ParseError, RpcRouter.Context<R>>;
};
/**
 * @since 1.0.0
 * @category combinators
 */
export declare const toHandlerRaw: <R extends RpcRouter<any, any>>(router: R) => <Req extends RpcRouter.Request<R>>(request: Req) => Rpc.Rpc.Result<Req, RpcRouter.ContextRaw<R>>;
/**
 * @since 1.0.0
 * @category combinators
 */
export declare const toHandlerUndecoded: <R extends RpcRouter<any, any>>(router: R) => <Req extends RpcRouter.Request<R>>(request: Req) => Rpc.Rpc.ResultUndecoded<Req, RpcRouter.Context<R>>;
//# sourceMappingURL=RpcRouter.d.ts.map